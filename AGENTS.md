<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Changelog

All notable changes to this project are documented below. Update this section with every change.

### [Unreleased]

- **Pastoral Care module (`/pastoral`) — notes, life events, risk scores, engagement.** Backend counterpart (all pastoral routes hardened with `@RequirePermissions`, new paginated risk/engagement list + member-scoring endpoints, recalc route switched to a permission guard) shipped same round — see backend changelog.
  - **New hooks** `src/hooks/use-pastoral.ts`: `PastoralNote` / `LifeEvent` / `RiskScore` / `EngagementScore` / `EngagementDistribution` / `MemberScoring` types mirroring the DTOs, display maps (`CONFIDENTIALITY_LABELS/TEXT`, `LIFE_EVENT_TYPES` + labels/text, `RISK_LEVEL_LABELS/TEXT`, `RISK_FACTOR_LABELS`, `ENGAGEMENT_BUCKET_LABELS/TEXT`, `ENGAGEMENT_FACTOR_LABELS`, `engagementBucketFor()`), and hooks — `usePastoralNotes`, `useCreatePastoralNote`, `useUpdatePastoralNote`, `useDeletePastoralNote`, `useLifeEvents`, `useCreateLifeEvent`, `useDeleteLifeEvent`, `useRiskScores`, `useEngagementScores`, `useEngagementDistribution`, `useMemberScoring`, `useRecalculateScores` (POST `/admin/dashboard/recalculate-scores`), all invalidating the `["pastoral-*"]` query-key family on writes.
  - **Notes hub `/pastoral`**: confidentiality filter, notes table (member, confidentiality badge, tags, line-clamped content, author, date), Add/Edit dialog (`MemberCombobox` + confidentiality select + comma-separated tags + content; member read-only when editing since PATCH can't move it), Delete confirm dialog — all write affordances gated `pastoral:create|update|delete`. `TablePagination`.
  - **Life Events `/pastoral/life-events`**: type filter + "Upcoming only" switch (upcoming auto-sorts by date ascending), sort (Event Date / Date Added + asc/desc), table with color-coded type labels and Notified/Pending badge, Add-life-event dialog (`MemberCombobox`, type select, date, optional notes stored as `{ notes }` details), Delete gated `pastoral:delete`.
  - **Risk Scores `/pastoral/risk-scores`**: debounced member-name search, level filter (critical/high/medium/low), sort by score or date + asc/desc, colored level badges, row click opens `MemberScoringDialog` (risk score + per-factor bars, suggested follow-ups via `useMemberScoring`), **Recalculate Scores** button gated `can("pastoral","update")` calling `useRecalculateScores` with a results toast.
  - **Engagement `/pastoral/engagement`**: four distribution `StatsCard`s (per-bucket counts) from `useEngagementDistribution`, bucket filter + debounced member search + sort, bucket badge table, row click opens `MemberScoringDialog`.
  - **Components** `src/components/pastoral/`: `note-form-dialog.tsx` (create+edit RHF/zod), `life-event-form-dialog.tsx` (create), `member-scoring-dialog.tsx` (risk/engagement factor bars + suggestions, reused by both scoring tables), `confirm-delete-dialog.tsx`.
  - **Docs**: `docs-nav.tsx` registers Pastoral Care (after Media); `docs-content.tsx` gains the Pastoral Care section (pages/actions/filters&sort) and a **Pastoral Care** row (`pastoral`) in the Permissions Matrix; Getting Started Main Menu lists Pastoral Care. Sidebar "Pastoral Care" section and `/pastoral*` route-permission map were already present.
  - Scoped eslint clean; tsc clean; build registers all four pastoral routes (static).

- **Header "Media" quick link is now permission-gated** (permission-gap audit follow-up): the horizontal header nav (`HORIZONTAL_NAV` in `header.tsx`) is now typed with an optional `permission?` field and rendered through a `canAny(...)` filter — the Media link only appears for `media:read` holders, matching the sidebar gate. The route-gate remained as backstop; users without access were already blocked at `/media`. Backend counterpart: the legacy path delete guard fix (see backend changelog). Full-repo lint clean (only the 6 pre-existing warnings); tsc clean; build passes.

- **Media Library module (`/media`) — library, upload, folders.**
  - **Backend counterparts**: media library routes hardened with `@RequirePermissions` (`media:read` on GET library/folders/single, `media:update`/`media:delete` on top of the `church_admin` role ceiling for permission-change/delete); upload endpoints stay auth-only — see backend changelog.
  - **New hooks** `src/hooks/use-media.ts`: `MediaAsset` type (mirrors `MediaAssetResponseDto` — assetId, filename, url, mimeType, sizeBytes, folder, permissions, createdAt), `ListMediaParams`, `UploadMediaResponse` (from `MediaResponseDto`), `MEDIA_PERMISSION_LABELS` / `MEDIA_PERMISSION_TEXT` maps, `classifyMime()` → image/audio/video/document, `formatBytes()`, paginated `useMediaLibrary(params)` (key `["media-library", params]`), `useMediaFolders()` (unwrap the `data` envelope → `string[]`), `useMediaAsset(id)`, and mutations — `useUpdateMediaPermissions`, `useDeleteMediaAsset`, `useUploadMediaFile()` (FormData; routes `image/*` → `/media/upload/image` auto-WebP, everything else → `/media/upload`) invalidating the media cache family. `MEDIA_UPLOAD_LIMITS` (image 5 MB / file 50 MB) mirrors backend caps.
  - **Library `/media`**: PageHeader w/ Upload button (`media:create` → `/media/upload`); stats cards (Total Files via limit=1 count, Folders); filter bar — type segmented control (All/Images/Audio/Video/Documents mapping to the server `mimeType` prefix `contains`), folder select (`useMediaFolders`), permission select (public/members/leadership), debounced filename search, sort (Date Added/Name/Size + asc/desc). Card grid: image thumbnails via plain `<img>` (remotePatterns not configured for `next/image`; `eslint-disable-next-line @next/next/no-img-element` per settings-tab convention) with click-to-open image preview dialog (Copy URL in footer), non-image files get FileAudio/FileVideo/FileText tiles; each card shows filename, folder + permission badges (color-coded text), size + date, overflow menu — Copy URL (`navigator.clipboard`), Change Permissions (`media:update` dialog w/ Public/Members/Leadership options), Delete (`media:delete` confirm dialog). Honors `?folder=` deep link via the Suspense + `useSearchParams` pattern from sermons. `TablePagination`. Write affordances hidden for read-only viewers.
  - **Upload `/media/upload`**: full-page `media:create` guard; folder select (existing folders + "+ New folder…" inline input, default `uploads`); drag-and-drop dropzone (drag feedback, click-to-browse, multi-file) with client-side size validation per type; queued file list with per-row status (waiting/uploading/done/error), sequential Upload All with error toasts + retryable rows; success rows show upload path + Copy URL; session upload history with View-in-Library/Clear actions.
  - **Folders `/media/folders`**: table of folders (name, file count badge + newest-file date via per-folder limit=1 queries, "View files" row click → `/media?folder=...`), empty state when no folders exist.
  - **`route-permissions.ts`**: `/media/folders` → `media:read` added (Library `/media` and Upload `/media/upload` were pre-wired).
  - **Docs**: `docs-nav.tsx` registers the Media section (after Sermons); `docs-content.tsx` gains a full Media module section (Pages: Library/Upload/Folders; Actions: Upload `media:create`, Copy URL `media:read`, Change Permissions `media:update`, Delete `media:delete`; Filters & Sort: type/folder/permissions/search/sort) and the Getting Started Navigation card Main Menu now lists Media; the Permissions Matrix Media row was already present.
  - Full-repo lint clean (0 errors, only the 6 pre-existing users/header warnings); tsc clean; build registers `/media`, `/media/folders`, `/media/upload` (all static); 564 backend tests pass.

- **Docs page gains the Sermons module + sermon list deep-link filtering.**
  - **`docs-nav.tsx`**: `DOCS_SECTIONS` registers `{ id: "sermons", label: "Sermons" }` (between Events and the Admin group) so the docs sidebar and scrollspy track it.
  - **`docs-content.tsx`**: new Sermons section (overview card; 6-row pages table — List/New/Detail/Edit/Series/Speakers; actions table — Add/Edit/Delete `sermons:create|update|delete`, Upload Audio-Video File `media:create`, Paste Media Link `sermons:create`, Export CSV `sermons:read`; filters-sort table — server search by title/speaker/scripture, sort by date/title/date-added, speaker/series deep-link filters). Permissions Matrix gains **Sermons** (`sermons`) and **Media** (`media`) rows; Getting Started Navigation card Main Menu line now lists Sermons.
  - **`/sermons` list-page deep-link fix**: arriving via `?speaker=` / `?series=` (from the Series/Speakers "View Sermons" links) now actually filters the table — the page shell wraps the content in `<Suspense>` (required by `useSearchParams` during static prerender) and the inner component seeds `speaker`/`series` state from the query, passes both into `useSermonsList`, and shows active-filter badges with a Clear button. `/sermons` still prerenders statically (`○`).
  - Scoped eslint clean (0 errors, only pre-existing users/header warnings); tsc clean; build registers all routes.

- **Sermons + media permission-gating completion.** Audit close-out ensuring every sermons action button/page is permission-guarded:
  - **Backend counterparts**: all 7 sermons routes now carry `@RequirePermissions` (`sermons:create/read/update/delete`), `media:create` granted to `branch_pastor` in the permissions seed — see backend changelog.
  - **`media-upload-field.tsx` now gates the upload affordance**: it reads `can("media", "create")` via `usePermissions()` and hides the **Upload File** pill (only **Paste Link** remains) when the caller can't upload — uploading creates a MediaAsset, which is a `media:create` action. Existing saved-URL rows and the Remove button stay visible either way; `route-permissions.ts` already declared `/media/upload` → `media:create`.
  - Verified gating remaining unchanged: list Add/Edit/Delete (`sermons:create/update/delete`), new/edit full-page guards, detail Edit button (`sermons:update`), hidden Actions column for read-only viewers. Bookmark/player affordances remain member-level (any `sermons:read` holder).
  - Scoped eslint clean (0 errors, only pre-existing users/header warnings); tsc clean; build registers all 6 sermon routes; 558 backend tests pass.

- **Sermon media attachments: audio/video file upload + external URL paste.** Sermons now support both self-hosted media (uploaded to Supabase Storage via `/media/upload`, saved as a MediaAsset) and external links (YouTube, SoundCloud, podcasts).
  - **Backend counterparts**: `sermons.video_url` column (migration `20260827100000_sermon_video_url`), media upload widened to audio/video MIME types with a 50MB cap, `videoUrl` on all sermon DTOs — see backend changelog.
  - **New component** `src/components/sermons/media-upload-field.tsx`: two modes toggled by pill buttons — **Upload File** (file picker filtered by `accept`, file name/size display, upload progress bar → `POST /media/upload` folder `sermons` → URL saved to sermon) and **Paste Link** (text input for external URLs saved directly to the sermon, no upload). Renders a "Saved: <url>" row with a Remove button once a value exists.
  - **`sermon-form.tsx`** gains a **Media** Card section: two side-by-side fields — **Audio Recording** (`AudioLines` icon, `accept="audio/*"`) and **Video** (`Video` icon, `accept="video/*"`). Uploaded/pasted URLs are included in the create/update payload (`audioUrl`/`videoUrl`) and pre-filled when editing. Duration inputs moved to a renamed "Duration & Description" card.
  - **`use-sermons.ts`**: `Sermon`, `CreateSermonInput` (and thus `UpdateSermonInput`) gain `videoUrl?: string`.
  - **Detail page `/sermons/[sermonId]`** now renders a **Video Player** card below the audio player when `videoUrl` is set: YouTube links (`youtube.com/watch`, `/shorts/`, `youtu.be/`) become an embedded `<iframe>` (16:9), anything else renders an HTML5 `<video controls>`.
  - Scoped eslint clean (0 errors, only pre-existing users/header warnings); tsc clean; 550 backend tests pass.

- **Sermons module (`/sermons`) — full CRUD, audio player, bookmarks, series, speakers.**
  - **Backend additions**: `GET /sermons/series` and `GET /sermons/speakers` aggregation endpoints returning distinct `series_name`/`speaker` values with sermon counts and last sermon date via Prisma `groupBy`. Both declared before `:sermonId` param route to avoid collision. Controller + service methods added.
  - **New hooks** `src/hooks/use-sermons.ts`: `Sermon` type (mirrors `SermonResponseDto`), `AggregatedItem` type, `ListSermonsParams`, `CreateSermonInput`/`UpdateSermonInput`, paginated `useSermonsList(params)` (key `["sermons-list", params]`), `useSermon(id)`, `useSermonsSeries()`, `useSermonsSpeakers()`, `useSermonBookmarks()`, `useIsBookmarked(sermonId)`, `useToggleBookmark(sermonId)`, and CRUD mutations (`useCreateSermon`, `useUpdateSermon`, `useDeleteSermon`).
  - **Shared form component** `src/components/sermons/sermon-form.tsx`: RHF+zod form with sectioned Cards — Sermon Details (title*/speaker/date/scripture), Series & Tags (series name, tag chips with add/remove), Media & Description (duration minutes:seconds, description textarea). Used by both create and edit pages.
  - **List `/sermons`**: stats cards (Total Sermons, Series count), debounced server search, sort controls (Date/Title/Date Added + asc/desc), paginated data table (title, speaker, date, series badge, duration with Clock icon, row actions View/Edit/Delete gated `sermons:update`/`sermons:delete`), ExportDropdown CSV export, delete confirmation dialog. Actions column hidden for read-only viewers.
  - **Create `/sermons/new`**: full-page `SermonForm`, page-level `sermons:create` guard, save navigates to `/sermons` on success.
  - **Detail `/sermons/[sermonId]`**: back button, bookmark toggle (Bookmark/BookmarkCheck icons, `useToggleBookmark`), Edit button gated `sermons:update`. Inline HTML5 audio player card (play/pause button, seek slider, time display). Info card (speaker, date, scripture, series, duration, tag badges). Description card when present.
  - **Edit `/sermons/[sermonId]/edit`**: page-level `sermons:update` guard, pre-fills `SermonForm` via `useEffect` reset, save navigates to detail page.
  - **Series `/sermons/series`**: aggregated table (series name, sermon count badge, last preached date, "View Sermons" link → `/sermons?series=...`). Empty state when no series exist.
  - **Speakers `/sermons/speakers`**: aggregated table (speaker name, sermon count badge, last spoke date, "View Sermons" link → `/sermons?speaker=...`). Empty state when no speakers exist.
  - **Route permissions**: `/sermons/series` → `sermons:read`, `/sermons/speakers` → `sermons:read`, `/sermons/[sermonId]/edit` → `sermons:update` added to route-permissions.ts.
  - Scoped eslint clean (0 errors, only pre-existing users/header warnings); tsc clean; build registers all 6 sermon routes; 550 backend tests pass.

- **Help & Documentation page (`/docs`).** New in-app documentation page accessible from the sidebar (SUPPORT section). Two-column SmartHR-style layout: sticky left sidebar nav with scrollspy + right content area with bordered cards. Documents all built modules (Members, Families, Visitors, Attendance, Giving, Events, Admin) with per-module sections covering: overview, pages (route + description table), actions (button + description + colored permission badge table), and filters/sort. Includes a full permissions matrix (resource × action grid) at the bottom. New files: `src/app/(dashboard)/docs/page.tsx`, `src/components/docs/docs-nav.tsx` (scrollspy + side nav), `src/components/docs/docs-content.tsx` (all content). Sidebar gains a new `SUPPORT` section with `BookOpen` icon. No permission gate — accessible to all authenticated users. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Visitor ticket assignment.** Tickets can now be assigned to visitors (existing or newly created during assignment). Schema: `tickets.visitor_id TEXT` column added via migration `20260826103830_ticket_visitor_id`. Backend: `CreateTicketDto` accepts optional `memberId`/`visitorId` (at least one required); `createTicket` handles both member and visitor paths (visitors skip EventRegistration creation); `listAllTickets` resolves `visitorName` alongside `memberName`. Frontend: new `VisitorCombobox` component + `useSearchVisitors` hook (reuses `GET /visitors?search=...`). `AssignTicketDialog` redesigned with Member/Visitor toggle tabs and inline "New Visitor" form (firstName/lastName/phone/email) that creates the visitor before assigning the ticket. `TicketRow` shows attendee with type badge (blue MemberCheck / purple UserPlus + "Visitor" badge). PDF shows `memberName || visitorName` as ATTENDEE. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Events permission gating sweep.** All action buttons across events/ticketing pages now require the correct permission. Route-level: added `/events/[eventId]/edit` → `events:update`, `/events/[eventId]/tiers` → `events:update`, `/events/list` → `events:read`, `/events/registrations` → `events:read` to route-permissions.ts. Component-level: management page "Assign Ticket" (`events:create`), "Create Ticket Type" / "Add First Type" (`events:create`), edit/delete tier buttons (`events:update` / `events:delete`), "+ Register new visitor" (`visitors:create`); tiers page "Add Tier" / "Add First Tier" (`events:create`), edit/delete tier buttons (`events:update` / `events:delete`); list page "Add Event" (`events:create`); edit page full "Access Denied" guard for `events:update`. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Events permission gating gaps closed.** Defense-in-depth fixes: create-event page (`/events/new`) now has a full-page `can("events", "create")` guard matching the edit/check-in pattern; add-event modal (`add-event-modal.tsx`) gains internal `usePermissions` check disabling the submit button when `!canCreate`; route-permissions.ts aligned `/events/check-in` from `events:update` to `events:create` to match the component's guard. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Tickets page redesigned: two tabs + PDF download.** Tickets page (`/events/management`) now has two tabs — **Ticket Types** (create/manage tiers per event with event selector, create/edit/delete tier dialog) and **Assigned Tickets** (table with code, event, member name, tier, price, status, used, date, plus per-row Download button). Assign Ticket dialog: event → tier → member → create. PDF generation via `jspdf` + `qrcode` — landscape event pass with QR code, event name/date/location, member name, tier, ticket code. Backend `listAllTickets` now resolves `memberName` and `eventLocation` via member join query. New dependency: `qrcode` + `@types/qrcode`. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Tickets page: renamed, manual ticket creation added.** Sidebar "Management" renamed back to "Tickets" with Ticket icon. Tickets page (`/events/management`) gains a "Create Ticket" button opening a dialog to manually assign a ticket to a member (event selector → tier selector if paid → member search via MemberCombobox → create). Backend endpoint `POST /events/:eventId/tickets` creates both registration (status `paid`) and ticket with generated code, validates tier capacity. `useCreateTicket` mutation hook added. Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Events ticketing, edit/delete, and management page.** Full event ticketing system with tier management, event edit page, global tickets management, and bug fixes.
  - **Bug fix**: Removed broken "Tickets" sidebar entry (`/events/tickets`) that collided with the `[eventId]` dynamic route, causing 404 errors. Replaced with "Management" entry pointing to `/events/management`.
  - **Event edit page** (`/events/[eventId]/edit`): full-page RHF+zod form pre-filled with existing event data, uses `useUpdateEvent` mutation, same sectioned-card layout as the create page (Event Details, Location & Capacity, Ticketing, Description).
  - **Event detail page**: Edit button wired with `onClick → /events/{eventId}/edit`. New "Manage Tiers" button (gated `events:update`) shown for paid events.
  - **Event detail modal** (`event-detail-modal.tsx`): now shows permission-gated Edit (navigates to edit page) and Delete (confirmation dialog) buttons in the footer.
  - **Cancel registration endpoint**: `DELETE /events/:eventId/register/:memberId` added to controller (service method existed but was not wired).
  - **Ticket tier management**: new `GET /events/:eventId/tiers`, `PATCH /events/:eventId/tiers/:tierId`, `DELETE /events/:eventId/tiers/:tierId` endpoints + service methods (`listTicketTiers`, `updateTicketTier`, `deleteTier`). Delete blocked when registrations reference the tier.
  - **Tier management page** (`/events/[eventId]/tiers`): table listing all tiers with name, price, capacity, description, inline create/edit dialog, delete confirmation.
  - **Global tickets management** (`/events/management`): `GET /events/management/tickets` endpoint (declared before `:eventId` to avoid collision) + frontend page with event/status/search filters, paginated tickets table (code, event, tier, price, status, used, date).
  - **New hooks** `use-events.ts`: `EventTicketTier` type, `useEventTiers`, `useCreateTier`, `useUpdateTier`, `useDeleteTier`, `AllTicketItem`/`AllTicketsResponse` types, `useAllTickets`.
  - **Sidebar**: "Tickets" → "Management" with Settings icon.
  - **Route permissions**: `/events/management` added to route-permissions.ts.
  - Scoped eslint clean (0 errors); tsc clean; 550 backend tests pass.

- **Events calendar dashboard + bug fixes.** Applied pending `attendance_event_id` migration. Fixed `useEventStats` crash (unsafe `att.data!` during loading). Fixed events list `meta.total` TypeError — `useEventsList` now returns `{ data, total }` matching the backend's actual response shape (was incorrectly typed as `PaginatedResponse` with a nonexistent `meta` wrapper). Rewrote `/events` dashboard with SmartHR-style FullCalendar layout.
  - **New dependencies**: `@fullcalendar/react`, `@fullcalendar/daygrid`, `@fullcalendar/timegrid`, `@fullcalendar/interaction`.
  - **New components** `src/components/events/`: `calendar-view.tsx` (FullCalendar wrapper with month/week/day switching, event-type color mapping, services rendered as recurring day-of-week slugs, skeleton loading, custom styled-jsx matching app CSS variables), `calendar-sidebar.tsx` (sticky card with mini datepicker via existing `Calendar` component, 6-item color legend, next-5 upcoming events list with typed left borders), `add-event-modal.tsx` (RHF+zod dialog: title/type/date/time/location/description, pre-fills date from calendar click, combines date+time into ISO), `event-detail-modal.tsx` (dark-header dialog with type/paid badges, date range, time range, location, registrations, price, description, "View Details" link).
  - **Dashboard `/events` rewritten**: two-column layout — sidebar (25%: datepicker, legend, upcoming) + calendar grid (75%). Date click opens AddEventModal with prefilled date. Event click opens EventDetailModal. Create button in header opens modal with no prefilled date.
  - **Events module (`/events`) — list, create, detail, registrations, check-in + attendance dashboard tie-in.** Backend counterpart (attendance.event_id FK, bulk/walk-in check-in endpoints, event attendance query) shipped same round — see backend changelog.
  - **New hooks** `src/hooks/use-events.ts`: full rewrite with `EventItem`/`EventRegistration`/`EventAttendanceRecord` types matching backend DTOs, `EVENT_TYPES` label map, paginated `useEventsList(params)` returning `EventsListResponse` (`{ data, total }`), `useEvent(id)`, `useEventRegistrations(eventId)`, `useEventAttendance(eventId)`, `useEventStats(eventId)` (derived from registrations + attendance), and mutations — `useCreateEvent`, `useUpdateEvent`, `useDeleteEvent`, `useRegisterForEvent`, `useCancelRegistration`, `useBulkCheckIn`, `useWalkInCheckIn`, `useValidateTicket`.
  - **Updated `src/hooks/use-attendance.ts`**: `RecordAttendanceInput` and `RecordBulkAttendanceInput` gain optional `eventId` (service or event required); `AttendanceRecord` type gains `eventId?`/`eventName?`.
  - **List `/events/list`**: debounced server search, type filter chips (All + 5 event types), sort controls (startDate/title/createdAt + asc/desc), paginated data table (title, type badge, date, location, registrations/capacity, row actions View/Edit/Delete gated by `events:update`/`events:delete`), ExportDropdown CSV export, delete confirmation dialog.
  - **Create `/events/new`**: full-page RHF+zod form with sectioned cards — Event Details (title/type*/startDate/endDate), Location & Capacity, Ticketing (isFree switch + conditional price), Description; save navigates to `/events` on success.
  - **Detail `/events/[eventId]`**: back link, header card (title, type badge, upcoming/past status, free badge, location, date range, capacity), 4-stat grid (Registered/Attended/No-Shows/Walk-Ins), Registrations tab (member ID, tier, payment status badge, checked-in badge, ticket code), Attendance tab (name, category, source, timestamp), Edit/Delete actions.
  - **Check-In `/events/check-in`**: event picker (upcoming events sorted by date), selected event info bar, two tabs — **Members (Roster)** (debounced search, checkbox rows, bulk action bar with "n selected" count → bulk check-in toast) and **Walk-In** (RHF+zod form: firstName/lastName/phone/email/gender → submit creates attendance record); recent attendance list below.
  - **Registrations `/events/registrations`**: event selector dropdown, stats row (Total/Checked In/Pending/Paid), registrations table (member, tier, quantity, payment status badge, checked-in badge, ticket code, date), client-side search filter.
  - **Route permissions** `/events/check-in` → `events:update`, `/events/new` → `events:create` added to `route-permissions.ts`.
  - **Sidebar** gains Check-In sub-item under Events.
  - **Attendance dashboard** gains Event Attendance section (recent events with registration counts, "View all" → `/events`).
  - Scoped eslint clean (0 new warnings); tsc clean; build registers all 6 event routes.

- **Unified table pagination with rows-per-page selector** across every paginated table. New shared `src/components/shared/table-pagination.tsx` ("Showing X–Y of Z {item}" + Rows-per-page select 10/15/25/50 + "Page p of N" + Prev/Next, hidden when empty) replaces the six hand-rolled Previous/Next footers — members, families, visitors, attendance services & records, and users. Each page swaps its fixed `perPage` const for state wired through the existing `limit` query param; changing the page size resets to page 1. No backend changes (limits already supported). Branches intentionally untouched (single-fetch, no footer today). Users page dropped three now-dead imports/vars in the swap. Scoped eslint clean (only the 4 pre-existing users-page warnings); tsc clean; build passes.

- **Giving module (`/giving`) — dashboard, categories, records, reports, recurring + service-level giving tie-in.**
  - **Backend counterpart** (transaction↔service/event linkage, `memberName`/`serviceName`/`eventName` on transactions and recurring rows, services `attendanceCount`, category-create role widening to treasurer/secretary, delete-service conflict on giving history, seed additions Gift/Venison/Overall Total) shipped same round — see backend changelog.
  - **New hooks** `src/hooks/use-giving.ts` (categories CRUD, transactions list, record-cash, send-receipt, recurring pause/resume/cancel with cache-family invalidation), minimal `src/hooks/use-events.ts` (event picker), `useService()` in use-attendance, and `api.getBlob()` for authenticated PDF downloads.
  - **Record Giving dialog** (`src/components/giving/record-cash-dialog.tsx`): Amount* · Method* (Cash/Bank transfer) · **Link-to segmented tabs — General (default) / Member / Service / Event** with the matching picker (MemberCombobox / service select / event select) · **Category*** select listing active categories plus a built-in **"Overall Total (day/event total)"** entry and **"+ Add new category…"** custom-typed creation (creates via POST; duplicate-name rejections resolve to the existing row) · Notes. Receipt number surfaces in the success toast.
  - **Records page**: filters (category/status/method/service/date-range), Linked column (Member/Service/Event badge + name), per-row receipt actions on successful gifts (**Download PDF** via getBlob, **Send to giver… → WhatsApp/Email**), export-everything, TablePagination.
  - **Dashboard `/giving`**: This-Month / All-Time totals derived from paged success fetches (works for any giving:read holder — the analytics endpoint stays role-restricted), 30-day giving trend area chart, recent-gifts list linking to Records.
  - **Categories page**: CRUD dialogs (name/description/display order/active toggle) gated by giving:create/update/delete; deactivate-only semantics matching the soft-delete backend; admin-only backend limits surface as error toasts for non-admins.
  - **Reports**: date range driving Total/Gifts/Categories stats, by-category horizontal bars, by-method split, export-everything CSV/PDF.
  - **Recurring page**: management table (giver name now server-resolved, frequency, next charge, failed-attempt badges) with Pause/Resume/Cancel confirmations gated `giving:update`; creation happens online and is documented as such.
  - **Attendance tie-in**: services table gains an **Attendance column** (all-time check-ins) and clickable rows → new **service detail page** `/attendance/services/[serviceId]` — header card (category badge, weekly schedule, last check-in), totals grid (check-ins / members / children / total giving ₦), attendance breakdown (adults vs children, members vs visitors), and a per-category giving bar chart for gifts tagged to that service.
  - Scoped eslint clean; tsc clean; build registers all six new routes.

- **Export-everything + attendance polish round**:
  - **"Export all rows" everywhere**: new `src/lib/export-all.ts` — `fetchAllPages()` walks a paginated endpoint sequentially at its cap (100/200) until the reported total is collected (hard ceiling 5,000 rows), and `listUrl()` serializes query params hook-style. `ExportDropdown` gains an optional async `fetchAllRows` prop: ticked rows still export instantly; with nothing selected it now fetches **every matching row** (filters/sort preserved, "Exporting..." state during the walk) instead of just the loaded page. Wired into members, families, visitors, users (`/profiles`), branches (its client-side Type filter re-applied in memory over the full fetch), the new attendance-records export, and reports (replacing its 200-row window). No backend changes — pagination caps already permit paging.
  - **Visitor detail → Visit History card**: check-ins linked via `attendance.visitor_id` listed (service, timestamp, category badge, source) with a total check-in count header; empty state when none.
  - **Check-in roster Load-more**: roster window starts at 50 active members and widens by 50 per click ("n remaining"), replacing the hard cap; search mode unchanged.
  - **Reports trend honors the date range** using the backend's new `startDate`/`endDate` trends params (card title switches between "last 30 days" and "selected range").
  - **Services dialog can clear Day/Start/End**: edit payload sends explicit `null`s (backend persists NULL); create keeps `undefined` semantics.
  - Five list pages (members/families/visitors/users/records) memoized their `queryParams` objects to satisfy exhaustive-deps after wiring the export callbacks.
  - Scoped eslint clean (only the 4 pre-existing users-page warnings); tsc clean; build registers all routes.

- **Attendance module (`/attendance`) — dashboard, services CRUD, check-in, records, reports.** Backend counterpart (paginated `GET /attendance`, `DELETE /services/:id` with attendance-conflict guard, `DELETE /attendance/:id`) shipped same round — see backend changelog.
  - **New hooks** `src/hooks/use-attendance.ts`: `ChurchService`/`AttendanceRecord`/summary/trend types mirroring the DTOs, `SERVICE_CATEGORIES` label map, services list/create/update/delete, `useAttendanceRecords(params)` (key `["attendance-records", params]`), record single/bulk mutations + `useDeleteAttendance` invalidating the records/summary/trends cache family, and summary/trends/by-service analytics queries.
  - **Route gates added**: `/attendance/check-in` → `attendance:create`; `/attendance/services` → `attendance:read`; `/attendance` → read (pre-wired).
  - **Dashboard `/attendance`**: stats cards (Total / This Month / Members / Visitors from two summary queries), first recharts usage via `ui/chart.tsx` — gradient area chart of the last 30 days' daily check-ins with formatted tooltip — plus a recent check-ins mini-list (name, service, time, Visitor/Category badges) linking to Records. Header quick-links to Services + Check-In for `attendance:create` holders.
  - **Services `/attendance/services`**: table (name, day-of-week label, HH:mm–HH:mm times, category badge, active dot-badge) with create/edit dialog (RHF+zod: name*, category select, day select with "Any day", time pickers, active switch), row activate/deactivate toggle (`update`), delete confirm dialog (`delete`) surfacing the backend conflict message when attendance exists; pagination footer.
  - **Check-In `/attendance/check-in`**: service picker whose category pre-fills an overridable category select; two modes — **Members (Roster)**: debounced search over active members (first page 50 / `useSearchMembers` ≥2 chars), checkbox rows feeding a "n selected" action bar → one bulk POST reporting "X checked in, Y skipped" (duplicates skipped server-side); **Visitor**: mini registration form (names*, phone, email, gender, custom fields editor) that creates the visitor then auto-records their check-in with `visitorId`, warns without losing the visitor if step 2 fails, and navigates to the new visitor's detail page.
  - **Records `/attendance/records`**: single mixed Member+Visitor list with Type column, service/category/date-range filters, source + timestamp columns, paginated; per-row delete gated `delete` with confirmation dialog.
  - **Reports `/attendance/reports`**: date-range card driving summary queries, stacked members-vs-visitors bar trend (30d), top-8 by-service horizontal bars derived client-side from the fetched window, gender split (derived male/female/unknown) and by-source tables, plus CSV export (date/time/name/type/service/category/source).
  - Scoped eslint clean; tsc clean; build registers all five routes.

- **Visitors module (`/visitors`) — list, full-page add, detail, follow-up board, convert flow.** Backend counterpart (camelCase DTOs, gender + custom fields on visitors, paginated list, permission hardening) shipped same round — see backend changelog.
  - **New hooks** `src/hooks/use-visitors.ts`: `Visitor` type mirroring `VisitorResponseDto` (incl. `gender`/`customFields`/`convertedMemberId`), `FOLLOW_UP_STATUSES` label map, `useVisitorsList(params)` (key `["visitors-list", params]` — server search/status/sort/pagination via `{data, meta}`), `useVisitor(id)`, and create/update/delete/convert mutations invalidating the visitor cache family.
  - **List page**: stats cards (Total / New This Month / In Follow-Up / Converted derived from one unfiltered limit=200 fetch), debounced server search, status filter (All + 6 statuses), sort (first/last name, first visit, status, date added) with asc/desc toggle; checkbox multi-select → batch Delete bar (`visitors:delete`, sequential with partial-failure toasts); ExportDropdown (selected-else-filtered: name, gender, contact, first visit, status, assignee resolved client-side via profiles, converted flag); row actions View/Edit(`visitors:update`)/Convert(`visitors:update`, hidden once converted)/Delete(`visitors:delete`); Actions column hidden for read-only viewers; pagination footer. Converted visitors show a badge in the name cell. Header has outline **Quick Add** dialog + primary **Add Visitor** → `/visitors/new`.
  - **Add page** `/visitors/new`: sectioned cards — Personal Information (names*, email, phone, WhatsApp, gender select male/female), Visit & Follow-Up (first-visit date defaulting to today*, initial status select defaulting New, assignee picker over active profiles), Custom Fields editor (reuses `CustomFieldsEditor`; empty-key rows dropped), Notes; save → navigate to the new visitor's detail page.
  - **Detail page** `/visitors/[visitorId]`: back link, header card (initials avatar, name, status badge, "Member Profile" badge linking to the converted member when present, first-visit/registered subline, gated Convert/Edit/Delete), Contact card, Visit & Follow-Up card with an **inline status Select gated `visitors:update`** (optimistic update, reverts + error toast on failure) and assigned-to name resolution, read-only Custom Fields card, Notes. Optimistic display state mirrors the member detail grammar.
  - **Follow-Up board** `/visitors/follow-up`: kanban columns New / Contacted / Scheduled / Interested / Converted·Dropped fed by a single unfiltered fetch and grouped client-side; cards show initials avatar, name (+Converted badge), visit recency ("Xd ago"), phone/email subline, assignee, column counts in headers, scrollable columns, client-side search filter; quick **status dropdown per card gated `visitors:update`** with optimistic cross-column moves that re-sync from cache on failure. **Drag-and-drop via @dnd-kit/core** (`PointerSensor` 6px activation covering mouse+touch, `KeyboardSensor`, `pointerWithin` collisions, `DragOverlay` tilted clone): each card has a GripVertical handle — the only element wired to drag listeners, so body clicks and the status select are untouched; columns are droppables highlighting with a primary ring on hover-over; drops route through the same optimistic move handler (same-status drops no-op). The merged terminal column keeps an existing `dropped_off`, anything else lands `converted`. All moves allowed, including out of terminal columns. An interim native-HTML5-DnD version was replaced same-day once registry access returned. The dropdown remains the pure-keyboard fallback.
  - **Components** `src/components/visitors/`: `visitor-form-dialog.tsx` (create + edit modes; edit sends every field so cleared values persist), `delete-visitor-dialog.tsx` (single/batch sequential delete with progress counter, per-item toasts ≤3, partial-failure summary, stays open on single failure — permanent-delete copy since there is no soft delete), `convert-visitor-dialog.tsx` (nullable `visitor` prop so an always-mounted instance renders nothing instead of crashing on closed state; prefilled names/email/phone + branch select; success toast notes gender/custom-fields carry-over and navigates to the new member).
  - Route gates for `/visitors*` were pre-wired in route-permissions.ts. Scoped eslint clean; tsc clean; build registers all four routes.

- **Sidebar active-state fix: longest matching link wins**. Leaf nav links now highlight only when their href is the *longest* match for the current path among all leaf links — previously the prefix rule (`startsWith(href + "/")`) kept index items like "All Members" (`/members`) lit on every subpage (`/members/new`, `/members/import`, `/members/families`). Implemented via a memoized leaf-href list from the permission-filtered tree + `isLinkActive` helper passed into `NavLink` (both desktop and collapsed/mobile render paths); parent highlighting and auto-expand behavior are unchanged. Same fix covers every section with index/subpage pairs (Sermons, Media, Visitors, ...). Deep routes without a dedicated link (e.g. `/members/{id}`) still highlight their section's index item. Scoped eslint/tsc clean; build passes.

- **Full-page Add Member (`/members/new`) + custom fields everywhere**.
  - **New page** with SmartHR sectioned-card grammar: Profile Photo (circular preview, Upload/Replace/Remove, image MIME ≤5 MB client validation, staged via object URL and uploaded only on save → `POST /media/upload/image` folder `members` → `PATCH /members/:id {photoUrl}`), Personal Information (names*, email, phone, WhatsApp, gender select, DOB), Address & Membership (address/city/state + branch select), Family Membership (optional: new `FamilyCombobox` + relationship select + head switch, disabled until a family is picked; link happens after create via `POST /families/:fid/members`, failure never loses the created member), Custom Fields editor, Notes, footer Cancel/Add Member. Save flow: create → optional photo (warning-toast on photo/family failure, still navigates) → success toast → `/members/{id}`.
  - **Custom fields are now CRUD-capable end-to-end** (backend already accepted the JSON): shared `src/components/members/custom-fields-editor.tsx` exports `customFieldsToRows` / `rowsToCustomFields` (empty-key rows dropped, duplicates last-wins, unknown values stringified) plus a repeatable Label+Value row editor for RHF `useFieldArray`. Wired into BOTH paths: `/members/new` (serialized into create payload) and `member-form-dialog.tsx` (edit pre-fills from stored fields and ALWAYS sends the record — `{}` when emptied — so deletions persist; quick-add dialog kept working as a second path). Member detail page gained a read-only **Custom Fields** card (Tag icon, DataRow grammar) shown only when entries exist.
  - **Supporting**: new `family-combobox.tsx` (Popover+Command over `useFamiliesList`, server search ≥2 chars, member-count sublines, Clear-selection); members list header now has primary **Add Member** (→ `/members/new`) beside outline **Quick Add** (existing dialog); `CreateMemberInput` gains `customFields`.
  - Route gate `members:create` for `/members/new` was pre-wired in route-permissions.ts. Scoped eslint/tsc clean; build registers `/members/new`.

- **Delete-confirmation sweep + family table polish + import upload redesign**.
  - **Confirmations for every destructive action**: audited all delete/deactivate flows. Two gaps closed — (1) users-page batch "Deactivate Selected" now opens `DeleteUserDialog` instead of firing instantly: the dialog was rewritten to accept `users: UserProfile[]` with the shared batch grammar (sequential deletes, progress counter "Deactivating... n/N", per-item toasts ≤3 items, partial-failure summary toast, single deletes stay open on failure; `onDeactivated` clears the selection). Single row deactivation passes `[user]` through the same dialog. (2) Family-detail "Remove" button no longer unlinks instantly — new `src/components/families/remove-family-member-dialog.tsx` (same alert-icon grammar) takes `familyId` + `familyName` + member and owns the mutation; page keeps a `removeTarget` state and renders the dialog. All other flows already had modals (members single/batch, branches list/detail, families list/detail).
  - **Family detail members table padding**: table now wrapped in `overflow-x-auto px-6 pb-5` with roomier cells (`[&_th]:py-3.5 [&_td]:py-4`), matching the app's card-table rhythm.
  - **Import upload step redesigned**: two-column layout — large drag-and-drop dropzone card (2/3) beside a dedicated "Need a template?" card (1/3). Dropzone has live drag feedback (primary border + tinted bg via `isDragging`, guarded against child-flicker on dragleave), icon-in-circle motif, format badges (.csv/.xlsx/.xls), and a ShieldCheck note that non-spreadsheet files are auto-rejected. File type is validated by extension *before* parsing (`validateFile()` used by both click-picker and drop handler) with a destructive "File rejected" toast. Template card lists every supported column as chips (required ones filled) plus a full-width primary **Download Template** button.
  - Scoped eslint clean (only the 4 pre-existing users-page warnings); tsc clean; build registers all routes.

- **Members subpages: Families (`/members/families`) + Import (`/members/import`)** — the two deferred Members routes are live.
  - **New hooks** `src/hooks/use-families.ts`: `Family` type (mirrors `FamilyResponseDto` incl. nested `members[]` with `relationship`/`isHead`), `useFamiliesList(params)` (key `["families-list", params]`; server search + pagination via `{data, meta}`), `useFamily(id)`, and create/update/delete/add-member/remove-member mutations invalidating both family cache families. Backend counterparts now enforce `families:*` permissions per endpoint.
  - **Shared picker** `src/components/members/member-combobox.tsx`: Popover+Command member selector — first-page load via `useMembersList({limit:20})`, server search ≥2 chars via `useSearchMembers`, `excludeIds` support, Clear-selection item; used for head-of-family and add-family-member pickers.
  - **Families list page**: stats cards (Total Families from a limit=1 count query; With Head / Members Linked from one unfiltered limit=200 fetch), debounced server search, checkbox multi-select → batch Delete bar (`families:delete`, sequential with partial-failure toasts), ExportDropdown (selected-else-filtered: name, head, count, member names, created); row actions View/Edit(`families:update`)/Delete(`families:delete`) with the Actions column hidden for read-only viewers; pagination footer.
  - **Family detail page** `/members/families/[familyId]`: back link, header card (initials avatar, name, member-count + Head badges, created date, gated Edit/Delete), Family Members table — each row links to `/members/:memberId`, relationship capitalized, Crown badge for the head, Remove button gated `families:update` (already-in-family members hidden from the Add picker), Add Member dialog (`memberId` + relationship select + is-head switch, all `families:update`). Optimistic display state mirrors the member detail grammar.
  - **Components** `src/components/families/`: `family-form-dialog.tsx` (RHF+zod: name required, optional head via MemberCombobox — create & edit modes), `delete-family-dialog.tsx` (single/batch sequential delete with progress counter; copy clarifies member records themselves are kept), `add-family-member-dialog.tsx`.
  - **Import wizard** `/members/import` (4 steps): Upload (.csv/.xlsx/.xls parsed client-side with the existing `xlsx` dep; drag-and-drop; downloadable template file) → Map (13 target fields incl. branch-by-name, alias-based auto-mapping) → Preview (client-side validation mirroring backend rules — required names, email format, status enum, branch-name resolution against `useBranchesList` — then server dry-run `POST /members/bulk-import {dryRun:true}` showing ready/skipped counts and up to 25 error rows) → Done (created count, skipped-row list, import-another / go-to-members). Commit call sends `dryRun:false`.
  - Sidebar items + route-permissions entries were already wired by earlier rounds. Scoped eslint clean; tsc clean; build registers `/members/families`, `/members/families/[familyId]`, `/members/import`.

- **Members module** (`/members`) — list, detail, create, edit, soft-delete + restore.
  - **New hooks** `src/hooks/use-members.ts`: `Member` type (mirrors `MemberResponseDto`), `useMembersList(params)` (key `["members-list", params]`, server-side search/status/branch/sort/pagination via `ListMembersParams`), `useMember(id)`, `useSearchMembers(q)` (enabled ≥2 chars), create/update/delete/restore mutations invalidating the member cache family, `useMemberGivingHistory` / `useMemberAttendanceHistory`, and `useAddMemberNote`.
  - **List page**: StatsCards (Total / Active / Other Statuses from two unfiltered limit=1 count queries), debounced server search, Status + Branch filters and sort (first/last name, member since, status, date added) all server-side with page state; checkbox multi-select → batch Deactivate bar (`members:delete`, sequential deletes); ExportDropdown exporting selected-else-filtered rows (name, contact, address, branch name resolved client-side, status, member since); row actions menu — View Details always, Edit + Restore-to-active `members:update`, Deactivate `members:delete`; Actions column hidden entirely for read-only viewers (same rule as users page); pagination footer. Member cell shows photo avatar/initials + email subline; status badge color-coded (active green / inactive gray / suspended red / transferred blue).
  - **Detail page** `/members/[memberId]`: back link, header card (avatar, name, status badge, branch + member-since subline, gated Deactivate/Edit buttons), Contact + Location/Membership info cards, Giving History and Attendance History cards (top-5 rows with record counts), Notes card showing persisted notes plus an add-note textarea for `members:update` holders. Optimistic display state from dialog saves.
  - **Shared components** `src/components/members/`: `member-form-dialog.tsx` (RHF+zod — first/last names required, email validated, phone/WhatsApp/DOB/gender/address/city/state/branch select/notes; status select in edit mode only) and `delete-member-dialog.tsx` (single/batch sequential deactivate with progress counter, per-item toasts, partial-failure summary, stays open on single failure).
  - Sidebar Members items + route-permissions map were already wired by the permission round. Scoped eslint clean; tsc clean; build registers both routes.

- **Action-button gating rolled out**: Users page (Invite → `users:create`; batch Deactivate + row Deactivate/Reactivate → `users:delete`; Edit Role/Reset Password/Force Sign Out → `users:update`; Actions column hidden entirely for read-only viewers), row actions in `user-columns.tsx` (overflow menu hidden when empty). Roles pages: Add Role, Reset to Defaults, matrix toggle/save bar gated by legacy role check (`church_admin`/`super_admin` — no dedicated roles resource). Church Settings page + GeneralSettingsTab swapped legacy `useCanManageChurch()` → `can("church_settings","update")` (Security tab is self-service, ungated). Branches list/detail split the coarse any-of flag into exact per-button checks (Add→create, Edit→update, Deletes→delete); 4 pre-existing lint warnings in users/page.tsx unchanged.

- **Permission system (frontend)**: permissions now gate pages, sidebar links, and action buttons. `src/hooks/use-permissions.ts` (`usePermissions`, `useCan`, `useRefreshPermissions`; fail-closed), `src/components/shared/can.tsx` (`<Can perm="...">`), `src/lib/route-permissions.ts` (longest-prefix route→permission map incl. legacy role rules for dashboards + Roles & Permissions page), `src/components/shared/permission-route-gate.tsx` mounted in `(dashboard)/layout.tsx` (skeleton while loading → EmptyState "Access denied"). Sidebar recursively filtered by `NavItem.permission` + legacy `NavItem.roles` (parents need ≥1 surviving child; empty sections dropped). `CurrentProfile` gains `permissions?: string[]`; `["current-profile"]` query refetches on window focus/reconnect for live updates; login flow primes the cache via `ensureQueryData` before navigating; logout clears it via existing `queryClient.clear()`. Branches list/detail swapped `useCanManageChurch()` → `can("branches", update|create|delete)`.

- **2026-08-23** — Country → dependent State dropdowns (branches + church settings), powered by `country-state-city`.
  - **Shared module** `src/components/shared/country-state-select.tsx`: `CountrySelectField` (all countries sorted A–Z, values are stored names matching DB rows) and `StateSelectField` (resolves the sibling country's ISO internally → its states; disabled until a country is chosen). Both take a `variant="info"` mode that renders in the settings 150px-label row grammar vs stacked dialog labels. State auto-clears when the country changes and the current value is invalid (`shouldDirty: true`, guarded against first-mount clearing); unknown stored values stay selectable so legacy data always displays.
  - **Branch form dialog** gains a Country select (defaults "Nigeria", paired with Phone in a 2-up grid, City + State above) and State becomes dependent; payload sends `country`. Detail page Location card shows Country. Branch list export gains a Country column.
  - **Church Settings General**: State/Country text inputs replaced with the same selects (`variant="info"`, honoring the read-only gating); dirty-only PATCH semantics unchanged.
  - Backend counterpart: branches got a `country` column (default "Nigeria"), and `UpdateBranchDto` was rewritten as `PartialType(CreateBranchDto)` fixing 400s on branch edits (see backend changelog).
  - Scoped eslint clean; tsc clean; build registers both routes.

- **2026-08-23** — Branch management (`/admin/branches`) — list, detail, create, edit, delete.
  - **New hooks** `src/hooks/use-branches.ts`: `Branch` type (matches `BranchResponseDto` incl. `memberCount`), `useBranchesList({search, sortBy, sortOrder, limit})` (key `["branches-list", params]` — correctly typed against the service's `{ data, total }` return, unlike the legacy `useBranches()` in use-users whose `meta` typing never matched the wire format; that hook is left untouched for the user-detail selector), `useBranch(id)`, and create/update/delete mutations invalidating both branch cache families.
  - **List page** mirrors the users-page grammar: stats cards (Total / Headquarters / Members Covered / Locations), server-side search (name·city·address) + sort (Name/City/Date Added + asc/desc toggle), client-side Type filter (All/HQ/Branches) over a single limit=100 fetch, checkbox multi-select with a "n selected" batch bar (Delete Selected → confirm dialog, Clear Selection), ExportDropdown exporting selected rows when any are ticked else all filtered rows (CSV/PDF/XLSX: name, email, phone, address, city, state, HQ Yes/No, members, created). Row click opens detail; per-row pencil edit + ⋯ menu (View/Edit/Delete); HQ badge on avatar cell; write affordances gated by `useCanManageChurch()` (readers get table only).
  - **Detail page** `/admin/branches/[branchId]` (`params: Promise` + `React.use`, matching users detail): back button + breadcrumb Home › Church Settings › Branches › name; header avatar tile + name + Headquarters badge; Contact / Location / Membership / Record Details bordered info cards (icon label-value rows); Edit (dialog) and Delete buttons for managers. Optimistic display state from dialog save responses so edits reflect instantly.
  - **Shared components** `src/components/branches/`: `branch-form-dialog.tsx` (RHF+zod: name required, email optional-but-valid, Switch headquarters toggle w/ "only one allowed" hint, empty-string→undefined normalization; used for both create and edit) and `delete-branch-dialog.tsx` (single or batch; sequential deletes with progress count, per-item error toasts surfacing the backend "has members assigned" message, stays open when a single delete fails, partial-failure summary toast for large batches).
  - **Sidebar**: Church Settings regains children — General (`/admin/settings`) + Branches (`/admin/branches`; GitBranch icon already present in ICON_MAP). Header gear/dropdown still land on General. Middleware `/admin` prefix already covered the new routes.
  - Backend note: `super_admin` added to all branch endpoints' `RequireRoles` (was church_admin-only for writes — platform admins previously got 403 everywhere). Scoped eslint clean (0 warnings); tsc clean; build registers both routes.

- **2026-08-23** — Settings consolidated into ONE page with two client-side tabs (General · Security).
  - **`/admin/settings`** is now the only settings route. The solid top-tab strip switches between **General Settings** and **Security** with React state — no navigation; the sticky left settings-list shows the active tab's sections (scrollspy tolerates tab swaps via its MutationObserver scan). The per-route layout and `security/` route were removed; the shell lives in `page.tsx`, hosting tab content components.
  - **New tab-content components** under `src/components/settings/`: `general-settings-tab.tsx` and `security-settings-tab.tsx` (markup carried over from the former routes).
  - **General → merged "Church" section** (`id="church"`): logo upload tile on top, then a single form where **Church Name + Denomination are permanently disabled** (fixed at registration) while Phone / Website / Address / City / State / Country stay editable — one Cancel/Save footer, dirty-only PATCH (locked fields never enter the payload). Email row + change dialog and Preferences unchanged below it. Left nav: Church · Email · Preferences.
  - **Website Settings tab/route deleted** — branding + public info moved into General's Church section; sidebar "Church Settings" becomes a plain link to `/admin/settings`. Header gear/dropdown unaffected; middleware already covers `/admin`; zero backend changes.
  - `settings-nav.tsx` reworked to id-keyed tabs (`"general" | "security"`) with stateful `SettingsTabsNav`. Scoped eslint clean; build registers the single `/admin/settings` route.

- **2026-08-23** — Settings hub restructured into three SmartHR-style tabs: General / Security / Website.
  - Superseded same day: first by the General/Security/Website split, then by the single-page two-tab consolidation at the top of this changelog.
  - **Shared shell** `src/app/(dashboard)/admin/settings/layout.tsx`: breadcrumb + solid top-tab strip (General Settings · Security · Website Settings, active from pathname), member/branch badges + read-only hint, and the two-column grid — sticky left settings-list card (per-tab section anchors) hosting the active page in one tall content card.
  - **New shared modules** under `src/components/settings/`: `ui.tsx` (`SettingsSection` anchored bordered groups carrying `data-settings-section`, `FieldRow`, generic `InfoField`), `settings-nav.tsx` (tab/side-nav config maps + `useScrollSpy()` that MutationObserver-scans `[data-settings-section]` so sections mounted after data loads are tracked), and `change-password-dialog.tsx` (extracted from `/profile`, now shared by both surfaces).
  - **General Settings** (`/admin/settings`): Church Information form slims to Name + Denomination; unified Email row + change dialog and Preferences (timezone/currency) unchanged.
  - **Security** (`/admin/settings/security`): personal account rows per security-settings.html grammar — Password (Change → shared dialog), Two-Factor Authentication (MFA status badge from current profile), Email Verification (verified address; changes point to General).
  - **Website Settings** (`/admin/settings/website`): Branding upload tile moved here + new Public Information always-editable form (Phone, Website, Address, City, State, Country) with dirty-only PATCH /church — contact/location fields moved out of General so each tab owns one concern.
  - **Sidebar**: Church Settings children are now the three real pages; dead Branches/Notifications/Webhooks/Custom Fields links removed. Header gear/dropdown unchanged (land on General). Middleware `/admin` prefix already covers the new routes; zero backend changes.
  - `useCanManageChurch` moved into `src/hooks/use-church.ts` for reuse by the layout. All scoped eslint clean; build registers all three routes.

- **2026-08-23** — `/admin/settings` restyled after SmartHR `business-settings.html`.
  - Superseded same day by the settings-hub restructure above (single General page → three-tab shell); the section grammar, upload tile and always-editable forms carried over into the shared modules.
  - **Layout grammar**: breadcrumb header → single-active "General Settings" top-tab strip (sibling pages slot in later) → two-column grid: sticky left card with a vertical settings-list (Branding · Email · Preferences · Church Information) + ONE tall content card whose sections are separated by border-bottoms, headed "Church Settings".
  - **Scrollspy**: `useScrollSpy()` (IntersectionObserver, `-96px/-66%` rootMargin) highlights the active section in the left list with an arrow icon; sections use `scroll-mt-28` for anchor offset. Mobile stacks the nav above content.
  - **Church Information is now always-editable inline** (SmartHR style): horizontal label-left/input-right rows (`FieldRow`/`InfoField`, 150px label column), 2-up column split on lg (Basic info | Address block), inputs disabled for non-admins; view↔edit toggle and read-mode DataRows removed. Dirty-only PATCH retained; Cancel (resets, disabled when clean) + Save Changes footer sits at the card bottom.
  - **Branding** is now an upload tile — muted rounded box, dashed-border 80px logo preview, name + size hint, small primary Upload/Change button.
  - **Email** keeps its change dialog but renders as a bordered section row (Mail tile + value + hint text + Change button). **Preferences** adopts the same row grammar with its own scoped Save.
  - Shared chrome extracted to `SettingsSection` (id + h3 heading + borders) and `FieldRow`; loading skeleton adapted to the grid; error state keeps retry. Scoped eslint clean; build passes.

- **2026-08-23** — Unified email model + header IA cleanup.
  - **Email is admin-managed in one place**: `/profile` shows Email read-only again ("Managed by your church admin in Settings") — schema/payload/hook types dropped it; `UpdateCurrentProfileInput` no longer has `email`. `/admin/settings` gains an **Email card** (Mail icon, Edit dialog, zod) calling new `useUpdateChurchEmail()` → `PATCH /church/email`, which updates sign-in credential + profile record + church contact together and invalidates both `["church"]` and `["current-profile"]` caches (header identity follows instantly). Church Information form no longer has its own Email input.
  - **Header dropdown** (desktop + mobile): "Account Settings" → back to **My Profile** (`/profile`); "Settings" → **Account Settings** (`/admin/settings`); removed the dead **"My Account"** item (no such route) and its unused icon import. Dropdown now: My Profile · Account Settings · Knowledge Base · Logout.
  - **Fix**: the read-only Email note on `/profile` renders as a plain muted `<p>` — `FormMessage` outside a `<FormField>` dereferences an undefined field context (`ui/form.tsx`) and would crash the edit form at runtime.
  - Backend suite 513 passing; frontend scoped eslint clean, build passes.

- **2026-08-23** — Church Settings page (`/admin/settings`) + Account Settings relabel.
  - **New hooks** `src/hooks/use-church.ts`: `useChurch()` / `useUpdateChurch()` (key `["church"]`, cache-set), `useChurchConfig()` / `useUpdateChurchConfig()` (key `["church-config"]`, merge-set on save), and `uploadChurchLogo()` (multipart POST `/media/upload/image`, folder `churches`).
  - **Page grammar** mirrors the SmartHR settings pages — breadcrumb "Settings", sectioned cards with bordered headers, right-aligned saves, read-only when the viewer isn't church_admin/super_admin (roles normalized from the auth profile):
    - **Branding**: logo preview (placeholder tile when unset) with Upload/Replace (image/* ≤5 MB) → optimized WebP URL saved via dirty-only-style PATCH.
    - **Church Information**: view ↔ edit toggle (Name*, Denomination, Email, Phone, Website, Address, City, State, Country) — RHF+zod inline form, generic per-field diff against defaults sends only changed keys.
    - **Preferences**: Timezone (Africa/Lagos default) + Currency (NGN default) selects persisted as config keys in one `PATCH /church/config { config }`; Save enabled only when dirty.
    - Header row shows member/branch count badges; skeleton loading + retry error state.
  - **This lights up every previously-dead settings link**: sidebar "Church Settings → General", header gear icon, dropdown/mobile "Settings" entries all point here now. Sibling children (Branches/Notifications/Webhooks/Custom Fields) remain 404 until built.
  - **Account Settings relabel**: header dropdown + mobile menu "My Profile" → "Account Settings" (href stays `/profile`), matching the chosen IA of one account page covering personal info, photo and security.
  - Scoped eslint clean (only pre-existing header warnings); build passes (`/admin/settings` registered).

- **2026-08-23** — My Profile follow-ups: editable email + full-width layout.
  - **Email is now editable** in Basic Information (backend `PATCH /profiles/me` accepts it and syncs Supabase Auth — see backend changelog): zod email validation, included in the dirty-field-only PATCH; the previous disabled "managed by your sign-in account" note removed. `UpdateCurrentProfileInput` gains `email?`.
  - **Width fix**: removed the `max-w-5xl` cap from the `/profile` container so the page fills the desktop content area like other dashboard pages.
  - Scoped eslint clean; build passes.

- **2026-08-22** — My Profile page (`/profile`) — view, edit, photo, password.
  - **New hooks** `src/hooks/use-profile.ts`: `useCurrentProfile()` (GET `/profiles/me`, key `["current-profile"]`), `useUpdateCurrentProfile()` (PATCH, cache-set on success), `useUploadAvatar()` (multipart POST `/profiles/me/photo`), `useChangePassword()` (PUT `/auth/password`). `src/lib/api.ts` now passes `FormData` bodies raw (browser sets the multipart boundary) instead of JSON-stringifying.
  - **Page** follows the SmartHR profile grammar — section cards with bordered headers and right-aligned Cancel/Save: identity card (avatar/initials, name, status + MFA badges, role labels incl. blue Custom via `resolveRoleLabel`, email/phone/branch/church/joined info rows); **Basic Information** card toggles read rows ↔ RHF+zod inline form editing First/Last/Phone only (email disabled — auth-managed), dirty-field-only PATCH; **Profile Photo** card with client-side validation (image MIME, ≤5 MB) + Replace/Upload button; **Security** card with MFA status row and a Change Password dialog (current/new ≥8/confirm match, SmartHR-style eye toggles).
  - **Wiring**: `/profile` added to `protectedPaths` in `middleware.ts` (the header's "My Profile" links existed but the route didn't); header dropdown + mobile menu now render real avatar/initials/name/email from `useCurrentProfile()` instead of hardcoded "AD" / "Admin User".
  - Deferred: email changes (Supabase-managed) and self-service MFA enable/disable flows (status display only). Scoped eslint clean; build passes.

- **2026-08-22** — GitHub Actions CI/CD (`.github/workflows/ci.yml`).
  - Runs on every push to any branch plus PRs to `main`; concurrency group cancels superseded runs on the same ref.
  - **Jobs**: `lint` (npm run lint) → parallel `typecheck` (`npx tsc --noEmit`) + `build` (next build, telemetry disabled); independent informational `audit` job (`npm audit --omit=dev`, continue-on-error). Mirrors the backend workflow conventions (Node 22, npm cache, actions v4).
  - **eslint.config.mjs**: added global `ignores` for `.next/**`, `out/**`, `node_modules/**`, and `next-env.d.ts` — ESLint was scanning compiled build output (183 false errors). Full-repo `npm run lint` is now clean (0 errors).

- **2026-08-22** — Fixed "Customized" stats card not counting custom roles.
  - The card only summed `isCustomized` (templates with church overrides); church-owned roles (`isChurchOwned`) were excluded. Now counts the union so newly added custom roles increment it immediately.

- **2026-08-22** — Role labels shown everywhere (fix: custom roles displayed as slugs).
  - **Root cause**: the friendly label ("Media Team") was never persisted — only the slugified `name` survived, and `getRoleLabel()`'s static dictionary only covers the 8 seeded roles. Backend now stores/returns `label` (see backend changelog).
  - **New hooks** in `use-roles.ts`: `useRoleLabelMap()` (roleName → display label from roles summary) + `resolveRoleLabel(name, map)`; `useAssignableRoles()` merges seeded `VALID_ROLES` with church-owned custom roles for pickers/filters; `RoleWithPermissions` and `RoleInfo` gain `label`.
  - **Surfaces updated**: roles overview table + matrix page header/breadcrumb/toasts prefer `role.label`; users list badges (`UserRoleCell`), detail sidebar badges, permission-matrix "Granted by" tooltips, current-roles chips; users page role filter dropdown now includes custom roles; invite/edit-role dialog selects include custom roles; CSV export resolves labels.
  - Scoped eslint clean (only pre-existing warnings); build passes.

- **2026-08-22** — Per-church custom roles (create + manage).
  - **Add Role flow**: "Add Role" button on `/admin/roles` opens `create-role-dialog.tsx` (RHF + zod: Label 3–50 chars required, Description ≤200 optional) posting to the new `POST /church/roles`. Live slug preview mirrors backend slugification ("Media Team" → `media_team`); reserved/duplicate names surface as 409 error toasts. On success: toast + navigate straight into the new role's matrix editor to configure permissions.
  - **Custom badge (blue)** for church-owned roles in the overview table and detail header (`isChurchOwned` from API), distinct from amber `Customized` (templates with overrides). Owned roles shadow same-named global templates.
  - **Matrix editor semantics per role type**: templates keep additive-only copy/warning toast; owned roles show "This role's own permissions" alert — ticks/unticks save as an absolute replace (revocations apply immediately). Reset button/dialog hidden for owned roles (nothing to reset).
  - **Hooks**: `useCreateRole()` added; `RoleWithPermissions.isChurchOwned?` typed.
  - Overview table wrapped in `overflow-x-auto px-4 pb-4` for horizontal-scroll padding parity with the users table. Lint clean; build passes.

- **2026-08-22** — Roles & Permissions admin pages (`/admin/roles`).
  - **Overview page**: stats cards (total roles, customized count, available permissions), role table sorted by `ROLE_ORDER` with permission counts ("x / 88"), description, and status badges — `Locked` (super_admin), amber `Customized` (church overrides exist), or `Default`. Row click navigates to the detail page. Wires up the previously-dead "Roles & Permissions" sidebar link.
  - **Matrix editor page** (`/admin/roles/[roleName]`): interactive resource × action checkbox grid (two-column alphabetical split, same visual grammar as the user-detail matrix) built from `useAllPermissions()`. Header card shows role label/description/badges and granted count; super_admin renders a read-only locked view with an explanatory alert.
  - **Additive-only semantics made explicit**: info alert explains ticks grant extra permissions for this church only and global defaults can't be revoked. On save, if unchecked permissions remain active (global defaults), a warning toast names the count and the selection re-syncs to the server's response. Empty selections are blocked (API requires ≥1 override id). Dirty-state floating save bar shows `+n / −n` with Discard.
  - **Guardrails mirrored from backend**: `PROTECTED_ADMIN_PERMISSIONS` cells are non-removable when editing church_admin (toast explains); Reset to Defaults button only enabled for customized roles and opens a confirm dialog explaining all church overrides are deleted; reset disabled while dirty (save/discard first).
  - **New hooks file** `src/hooks/use-roles.ts`: types (`Permission`, `RoleWithPermissions`), `useRolesSummary` (GET `/church/roles`), `useRolePermissions(roleName)`, `useAllPermissions` (GET `/church/roles/all`), `useSetRolePermissions` (PUT `/church/roles/:roleName/permissions`), `useResetRoleToDefaults` (POST `.../reset`), plus `getResourceLabel`/`sortRolesByOrder` helpers. Re-exports `getRoleLabel` from use-users for consistent naming.
  - New components: `components/roles/permission-matrix-editor.tsx`, `components/roles/reset-role-dialog.tsx`. Scoped eslint clean on new files; build passes.

- **2026-08-22** — Compact permission matrix on user detail page.
  - **Role & Permissions tab redesign**: the Effective Permissions card no longer renders ~90 outline badges under per-resource headings. It now shows a dense permission matrix — one thin row per resource, four fixed columns (`create`/`read`/`update`/`delete`, extras appended if ever added), `Check` icon when granted, dimmed `Minus` otherwise. Two side-by-side matrices split alphabetically in a `lg:grid-cols-2` grid (stacks on mobile); header row labels each column.
  - **grantedBy tooltips**: hovering a ✓ cell (app-wide `TooltipProvider` from `providers.tsx`) shows "Resource: action" plus "Granted by <roles>" via `getRoleLabels(perm.grantedBy)` — previously-unused API data.
  - **Summary line**: CardDescription reads "<n> permissions across <m> resources" (hidden for super_admin, which keeps its short-circuit text; empty state unchanged).
  - New local `PermissionMatrix` component + `PERMISSION_ACTIONS` constant in `user-detail-content.tsx`; data memo reshaped to `Map<resource, Map<action, EffectivePermission>>`. Lint clean, build passes.

- **2026-08-22** — Multi-role users + editable user detail page.
  - **`UserProfile.role` is now `string[]`** (all roles, highest rank first; `role[0]` = primary). Added `roles?` (name + description), `effectivePermissions?`, `lastSignInAt?`, and `member?` (linked member summary) to the detail payload. `getRoleLabels()` joins all roles for display.
  - **New hooks**: `useUser`, `useUpdateUser(profileId)` (PATCH `/profiles/:id` — names, email, phone, branch, status), `useUpdateUserRoles(profileId)` (PATCH `/profiles/:id/roles` — full role set, permissions accumulate across roles), and `useBranches()` for the branch selector.
  - **Users list**: `UserRoleCell` renders one badge per role; CSV export lists all role labels via `getRoleLabels`.
  - **Detail page (`/admin/users/[profileId]`)**: Account Info tab now has an Edit mode (first/last name, email, phone, branch select, status select) with dirty-field-only PATCH payloads; linked Member record card shown when present; Role & Permissions tab has a toggleable multi-role chip picker plus effective-permission groups by resource (super_admin short-circuits to "all permissions"); Security tab shows real last sign-in from Supabase admin API.
  - **Sidebar/dialog**: sidebar lists every role as a badge (super_admin in destructive red); edit-role dialog defaults to the primary (`role[0]`) role.
